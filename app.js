//--------------------------------------------------------------// Standard setup//config filevar config = require('./config');
//express web applicationvar express = require('express');//web server instancevar server = express.createServer();//socket.io instancevar io = require('socket.io').listen(server);//stop rendering layout every timeserver.set("view options", { layout: false });//sets the view engine to render pages with jadeserver.set('view engine', 'jade');//this allows forms to be encoded to req.bodyserver.use(express.bodyParser());//--------------------------------------------------------------//start serverserver.listen(config.web.port);console.log("Started " + config.name + " (" + config.version + ")"); if(config.web.port != 80)    console.log("on " + config.web.servername + ":" + config.web.port + "/"); 